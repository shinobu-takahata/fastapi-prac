version: '3.8'

services:
  backend:
    build:
      context: ./app
      dockerfile: Dockerfile
    environment:
      POETRY_VIRTUALENVS_CREATE: "false"
    volumes:
      - ./app:/app
    ports:
      - "8000:80"
